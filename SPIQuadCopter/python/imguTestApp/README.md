Setup and run the imgui test app

Create a virtual environment and install dependencies:

```bash
python3 -m venv imgui-env
source imgui-env/bin/activate
pip install -r requirements-imgui.txt
```

Run the app:

```bash
python tang9k_imgui.py
```

Note: `imgui-bundle` may require additional system packages depending on your platform.
